space arch/aarch64/boot/dtb/parser/fdt_ir

form ParserState {
    offset: u64
    size: u64
    valid: bool
}

proc initialize() -> ParserState {
    give ParserState {
        offset: 0,
        size: 0,
        valid: true
    }
}

proc parse() {
    emit "fdt_ir parse"
}

proc validate() -> bool {
    give true
}

proc debug() {
    emit "fdt_ir debug"
}
