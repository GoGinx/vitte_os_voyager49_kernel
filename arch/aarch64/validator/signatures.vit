space arch/aarch64/boot/dtb/validator/signatures

form ValidationResult {
    valid: bool
    reason: string
}

proc validate() -> ValidationResult {
    give ValidationResult {
        valid: true,
        reason: "signatures ok"
    }
}

proc repair() {
    emit "signatures repair"
}

proc diagnostics() {
    emit "signatures diagnostics"
}
