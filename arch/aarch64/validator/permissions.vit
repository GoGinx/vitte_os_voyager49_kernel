space arch/aarch64/boot/dtb/validator/permissions

form ValidationResult {
    valid: bool
    reason: string
}

proc validate() -> ValidationResult {
    give ValidationResult {
        valid: true,
        reason: "permissions ok"
    }
}

proc repair() {
    emit "permissions repair"
}

proc diagnostics() {
    emit "permissions diagnostics"
}
