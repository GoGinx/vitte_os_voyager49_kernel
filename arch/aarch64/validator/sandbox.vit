space arch/aarch64/boot/dtb/validator/sandbox

form ValidationResult {
    valid: bool
    reason: string
}

proc validate() -> ValidationResult {
    give ValidationResult {
        valid: true,
        reason: "sandbox ok"
    }
}

proc repair() {
    emit "sandbox repair"
}

proc diagnostics() {
    emit "sandbox diagnostics"
}
