space arch/aarch64/boot/dtb/validator/gpio

form ValidationResult {
    valid: bool
    reason: string
}

proc validate() -> ValidationResult {
    give ValidationResult {
        valid: true,
        reason: "gpio ok"
    }
}

proc repair() {
    emit "gpio repair"
}

proc diagnostics() {
    emit "gpio diagnostics"
}
