space arch/aarch64/boot/dtb/validator/policies

form ValidationResult {
    valid: bool
    reason: string
}

proc validate() -> ValidationResult {
    give ValidationResult {
        valid: true,
        reason: "policies ok"
    }
}

proc repair() {
    emit "policies repair"
}

proc diagnostics() {
    emit "policies diagnostics"
}
