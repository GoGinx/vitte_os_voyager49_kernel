space arch/aarch64/boot/dtb/vendors/virtualbox

form VendorBoard {
    name: string
    compatible: string
}

proc initialize() {
    emit "virtualbox initialized"
}

proc probe() {
    emit "virtualbox probe"
}

proc register() {
    emit "virtualbox register"
}
