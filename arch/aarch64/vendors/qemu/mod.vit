space arch/aarch64/boot/dtb/vendors/qemu

form VendorBoard {
    name: string
    compatible: string
}

proc initialize() {
    emit "qemu initialized"
}

proc probe() {
    emit "qemu probe"
}

proc register() {
    emit "qemu register"
}
