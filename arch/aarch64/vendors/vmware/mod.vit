space arch/aarch64/boot/dtb/vendors/vmware

form VendorBoard {
    name: string
    compatible: string
}

proc initialize() {
    emit "vmware initialized"
}

proc probe() {
    emit "vmware probe"
}

proc register() {
    emit "vmware register"
}
