space arch/aarch64/boot/dtb/vendors/hyperv

form VendorBoard {
    name: string
    compatible: string
}

proc initialize() {
    emit "hyperv initialized"
}

proc probe() {
    emit "hyperv probe"
}

proc register() {
    emit "hyperv register"
}
