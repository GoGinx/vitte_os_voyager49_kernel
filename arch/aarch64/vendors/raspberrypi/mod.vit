space arch/aarch64/boot/dtb/vendors/raspberrypi

form VendorBoard {
    name: string
    compatible: string
}

proc initialize() {
    emit "raspberrypi initialized"
}

proc probe() {
    emit "raspberrypi probe"
}

proc register() {
    emit "raspberrypi register"
}
