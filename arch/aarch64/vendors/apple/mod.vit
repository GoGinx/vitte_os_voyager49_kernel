space arch/aarch64/boot/dtb/vendors/apple

form VendorBoard {
    name: string
    compatible: string
}

proc initialize() {
    emit "apple initialized"
}

proc probe() {
    emit "apple probe"
}

proc register() {
    emit "apple register"
}
