space arch/aarch64/boot/dtb/vendors/qualcomm

form VendorBoard {
    name: string
    compatible: string
}

proc initialize() {
    emit "qualcomm initialized"
}

proc probe() {
    emit "qualcomm probe"
}

proc register() {
    emit "qualcomm register"
}
