space arch/aarch64/boot/dtb/vendors/mediatek

form VendorBoard {
    name: string
    compatible: string
}

proc initialize() {
    emit "mediatek initialized"
}

proc probe() {
    emit "mediatek probe"
}

proc register() {
    emit "mediatek register"
}
