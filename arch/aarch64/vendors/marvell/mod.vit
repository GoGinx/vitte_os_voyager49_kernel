space arch/aarch64/boot/dtb/vendors/marvell

form VendorBoard {
    name: string
    compatible: string
}

proc initialize() {
    emit "marvell initialized"
}

proc probe() {
    emit "marvell probe"
}

proc register() {
    emit "marvell register"
}
