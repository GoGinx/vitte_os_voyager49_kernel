space arch/aarch64/boot/dtb/vendors/oracle

form VendorBoard {
    name: string
    compatible: string
}

proc initialize() {
    emit "oracle initialized"
}

proc probe() {
    emit "oracle probe"
}

proc register() {
    emit "oracle register"
}
