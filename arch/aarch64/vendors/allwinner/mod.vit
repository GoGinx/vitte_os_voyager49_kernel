space arch/aarch64/boot/dtb/vendors/allwinner

form VendorBoard {
    name: string
    compatible: string
}

proc initialize() {
    emit "allwinner initialized"
}

proc probe() {
    emit "allwinner probe"
}

proc register() {
    emit "allwinner register"
}
