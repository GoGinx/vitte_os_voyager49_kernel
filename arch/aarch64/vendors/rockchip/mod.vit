space arch/aarch64/boot/dtb/vendors/rockchip

form VendorBoard {
    name: string
    compatible: string
}

proc initialize() {
    emit "rockchip initialized"
}

proc probe() {
    emit "rockchip probe"
}

proc register() {
    emit "rockchip register"
}
